const userDao = require("./userDao");
const productDao = require("./productDao");
const cartDao = require("./cartDao");
const paymentDao = require("./paymentDao");

module.exports = { userDao, productDao, cartDao, paymentDao };
